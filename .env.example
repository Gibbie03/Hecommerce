# Local Postgres (see docs/SUPABASE_MIGRATION.md for moving to hosted Supabase)
DATABASE_URL=postgresql://icommerce:icommerce@localhost:5432/icommerce

# Session signing secret for the email-OTP auth cookie (any long random string)
SESSION_SECRET=dev-only-change-me-before-any-real-deployment

# Resend (https://resend.com) — required to actually send verification/OTP emails.
# Needs a verified sending domain; leave unset until you have one — endpoints
# degrade to a clear "not configured" response (code logged server-side instead).
RESEND_API_KEY=
RESEND_FROM_EMAIL=Icommerce <onboarding@icommerce.ng>

# Twilio (https://twilio.com) — required to actually send SMS sign-in codes and
# phone business-verification codes. Leave unset in local dev; same graceful
# "not configured" degradation as Resend above.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Supabase (https://supabase.com) — required for production only. Powers two
# privileged, server-only operations: creating auth.users via the Admin API
# and Storage uploads (see docs/SUPABASE_MIGRATION.md). Leave unset in local
# dev — both fall back to the local auth shim and public/uploads/ exactly as
# before. SUPABASE_URL is the project's API URL (https://<ref>.supabase.co);
# SUPABASE_SERVICE_ROLE_KEY is the service_role key from Project Settings >
# API — never expose it to the client, never commit it.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_STORAGE_BUCKET=business-images

# Base URL used in emails and for building absolute links
APP_URL=http://localhost:3000
